<UserControl x:Class="CommandosMissionEditor.Views.AddPatrolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:CommandosMissionEditor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <viewModels:AddPatrolViewModel/>
    </d:UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary Source="../ResourceDictionary.xaml" />
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Patrols" />
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Mission.World.EnemyPatrols}" SelectedItem="{Binding SelectedEnemyPatrol}" />
        <UniformGrid Grid.Row="1" Grid.Column="1" Rows="1">
            <Button Content="Edit Selected" Command="{Binding EditSelectedPatrolCommand}" />
            <Button Content="Remove Selected" Command="{Binding RemoveSelectedPatrolCommand}" />
        </UniformGrid>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Token Id" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.TokenId}" />
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Starting Position" />
        <UniformGrid Grid.Row="3" Grid.Column="1" Rows="1">
            <TextBlock Text="X" />
            <TextBox Text="{Binding CurrentEnemyPatrol.Position.X}" />
            <TextBlock Text="Y" />
            <TextBox Text="{Binding CurrentEnemyPatrol.Position.Y}" />
            <TextBlock Text="Z" />
            <TextBox Text="{Binding CurrentEnemyPatrol.Position.Z}" />
        </UniformGrid>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Area" />
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.Area}" />
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Angle" />
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.Angle}" />
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Columns Count" />
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.ColumnsCount}" />
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Rows Count" />
        <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.RowsCount}" />
        <TextBlock Grid.Row="8" Grid.Column="0" Text="Soldiers File Name" />
        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.SoldiersFileName}" />
        <TextBlock Grid.Row="9" Grid.Column="0" Text="Leader File Name" />
        <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding CurrentEnemyPatrol.LeaderFileName}" />
        <UniformGrid Grid.Row="10" Grid.Column="1" Rows="1">
            <Button Content="Add Patrol" Command="{Binding AddPatrolCommand}" />
            <Button Content="Clear Patrol Information" Command="{Binding ClearPatrolCommand}" />
        </UniformGrid>
    </Grid>
</UserControl>
