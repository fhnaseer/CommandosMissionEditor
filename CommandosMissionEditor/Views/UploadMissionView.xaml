<UserControl x:Class="CommandosMissionEditor.Views.UploadMissionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:CommandosMissionEditor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:UserControl.DataContext>
        <viewModels:UploadMissionViewModel />
    </d:UserControl.DataContext>
    <StackPanel>
        <Label Content="File Path (.mis)" />
        <TextBox Text="{Binding MissionFilePath}" />
        <Button Content="Load Mission" Command="{Binding LoadMissionCommand}" />
    </StackPanel>
</UserControl>
